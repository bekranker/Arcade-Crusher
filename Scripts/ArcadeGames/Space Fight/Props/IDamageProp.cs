public interface IDamageProp
{
    /// <summary>
    /// Applies damage to the prop.
    /// </summary>
    /// <param name="amount">The amount of damage to apply.</param>
    void ApplyDamage(float amount);

}